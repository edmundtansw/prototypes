jdbc:
  url: jdbc:mariadb://localhost:3306/edge?rewriteBatchedStatements=true
  username: root
  password:
  driver-class: org.mariadb.jdbc.Driver
  test-on-borrow: true
  test-while-idle: true
  validation-query: SELECT 1
  session-table: sessions
  max-idle: 1
  max-connections: 10000